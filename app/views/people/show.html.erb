<!-- firehose track 7, step 18 - file created 3 aug 17 for solo GenET project person details page -->

<div class="booyah-box col-10 offset-1">
  <h1><%= @person.first_name %> <%= @person.middle_name %> <%= @person.last_name %></h1>
  <h2><%= @person.sex %></h2>
  <h2><%= @person.birth_date %></h2>
  <h2><%= @person.death_date %></h2>
  <h2><%= @person.notes %></h2>
  
<!-- edit button added 5 aug 17 for editing person details (step 19) -->
  <div class="float-right">
    <%= link_to 'Edit', edit_person_path(@person), class: 'btn btn-primary' %>
  </div>
</div>
